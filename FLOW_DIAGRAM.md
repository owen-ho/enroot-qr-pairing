# 🎨 System Flow Diagram

## Participant Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     PARTICIPANT JOURNEY                       │
└─────────────────────────────────────────────────────────────┘

1️⃣ ARRIVAL
   ┌───────────┐
   │ Scan QR   │ ──► QR code at entrance
   │   Code    │     (one QR for all participants)
   └─────┬─────┘
         │
         ▼
   ┌───────────┐
   │  Open     │ ──► Browser opens webapp
   │  Webapp   │     http://SERVER_IP
   └─────┬─────┘
         │
         ▼

2️⃣ JOINING
   ┌───────────┐
   │   Click   │
   │   "Join   │ ──► POST /api/join
   │   Event"  │
   └─────┬─────┘
         │
         ▼
   ┌───────────┐
   │  Receive  │ ──► Example: "brave-penguin"
   │  Unique   │     Adjective + Animal
   │    ID     │
   └─────┬─────┘
         │
         ▼

3️⃣ PAIRING (Immediate)
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────┐
│Someone│ │  No   │
│Waiting│ │ One   │
│   ?   │ │Waiting│
└───┬───┘ └───┬───┘
    │         │
    │         ▼
    │    ┌──────────┐
    │    │  Show    │
    │    │ "Waiting │ ──► Poll every 4 seconds
    │    │for match"│     GET /api/status
    │    └──────────┘
    │         │
    │         ▼
    │    ┌──────────┐
    │    │  Toast   │
    │    │"Match    │ ──► When someone joins
    │    │ found!"  │
    │    └──────────┘
    │         │
    ▼         ▼
┌─────────────────┐
│   PAIRED! 🎉    │
│ Partner: "ID"   │
│ Meet at entrance│
└────────┬────────┘
         │
         ▼

4️⃣ CRAFTING
   ┌───────────┐
   │   Visit   │
   │  Stations │ ──► • Tote bag decoration
   │  Together │     • Origami crafts
   └───────────┘     • Bead bracelets
                     • Keychain painting

5️⃣ IF PARTNER LEAVES
   ┌───────────┐
   │   Click   │
   │ "Partner  │ ──► POST /api/unpair
   │   Left"   │
   └─────┬─────┘
         │
         ▼
   ┌───────────┐
   │ Auto      │ ──► Try to re-pair immediately
   │ Re-pair   │     If no one waiting, show "Waiting"
   └───────────┘
```

## Admin Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      ADMIN DASHBOARD                          │
└─────────────────────────────────────────────────────────────┘

1️⃣ ACCESS
   ┌───────────┐
   │   Open    │
   │  /admin   │ ──► http://SERVER_IP/admin
   └─────┬─────┘
         │
         ▼
   ┌───────────┐
   │   Enter   │
   │ Password  │ ──► POST /api/admin/login
   └─────┬─────┘
         │
         ▼

2️⃣ MONITORING
   ┌─────────────────────────────────┐
   │      REAL-TIME DASHBOARD        │
   │                                 │
   │  ┌─────┐ ┌─────┐ ┌─────┐      │
   │  │Total│ │Paired│ │Wait│      │ ──► Auto-refresh
   │  │  12 │ │  10  │ │ 2  │      │     every 5 sec
   │  └─────┘ └─────┘ └─────┘      │
   │                                 │
   │  Participant List:              │
   │  ┌────────────────────────────┐│
   │  │ happy-turtle | paired     ││ ──► View all
   │  │ brave-penguin| paired     ││     participants
   │  │ clever-fox   | waiting    ││
   │  └────────────────────────────┘│
   └─────────────────────────────────┘

3️⃣ MANUAL ACTIONS
   ┌───────────┐
   │   Pair    │ ──► Select 2 waiting participants
   │   Users   │     POST /api/admin/pair
   └───────────┘
         
   ┌───────────┐
   │  Unpair   │ ──► Break existing pairing
   │   Users   │     DELETE /api/admin/unpair/:id
   └───────────┘

   ┌───────────┐
   │   Reset   │ ──► Clear all data for new event
   │   Data    │     POST /api/admin/reset
   └───────────┘
```

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SYSTEM COMPONENTS                          │
└─────────────────────────────────────────────────────────────┘

FRONTEND (Port 80)
┌──────────────────────┐
│   React + Vite       │
│   ┌──────────────┐   │
│   │Participant   │   │ ──► React Router
│   │   Page       │   │
│   └──────────────┘   │
│   ┌──────────────┐   │
│   │   Admin      │   │
│   │   Page       │   │
│   └──────────────┘   │
│                      │
│  react-hot-toast     │ ──► Notifications
│  localStorage        │ ──► Session persistence
└──────────┬───────────┘
           │
           │ HTTP Requests
           │ (REST polling)
           ▼
BACKEND (Port 3000)
┌──────────────────────┐
│  Express Server      │
│                      │
│  ┌───────────────┐   │
│  │ Participant   │   │ ──► /api/join
│  │    Routes     │   │     /api/status
│  └───────────────┘   │     /api/unpair
│                      │
│  ┌───────────────┐   │
│  │    Admin      │   │ ──► /api/admin/*
│  │    Routes     │   │     (with JWT auth)
│  └───────────────┘   │
│                      │
│  JWT Authentication  │
│  unique-names-gen    │ ──► "brave-penguin"
└──────────┬───────────┘
           │
           │ SQL Queries
           ▼
DATABASE (Port 5432)
┌──────────────────────┐
│   PostgreSQL 15      │
│                      │
│  ┌───────────────┐   │
│  │ participants  │   │ ──► id, slug, token,
│  │               │   │     joined_at, status
│  └───────────────┘   │
│                      │
│  ┌───────────────┐   │
│  │   pairings    │   │ ──► id, participant1_id,
│  │               │   │     participant2_id,
│  └───────────────┘   │     paired_at, status
│                      │
└──────────────────────┘
```

## Pairing Algorithm

```
┌─────────────────────────────────────────────────────────────┐
│                   PAIRING ALGORITHM                           │
└─────────────────────────────────────────────────────────────┘

NEW PARTICIPANT JOINS
         │
         ▼
┌─────────────────┐
│ Generate unique │
│     slug        │ ──► "happy-turtle"
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Create JWT     │
│     token       │ ──► Contains: participantId, slug
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Insert to     │
│    database     │ ──► Status: 'waiting'
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Query: Any      │
│ waiting users?  │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    NO        YES
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│Return  │ │ Get    │
│without │ │oldest  │ ──► FIFO: First In, First Out
│partner │ │waiting │
└────────┘ │  user  │
           └────┬───┘
                │
                ▼
           ┌────────┐
           │ Update │
           │  both  │ ──► Status: 'waiting' → 'paired'
           │ status │
           └────┬───┘
                │
                ▼
           ┌────────┐
           │ Create │
           │pairing │ ──► Record in 'pairings' table
           │ record │
           └────┬───┘
                │
                ▼
           ┌────────┐
           │ Return │
           │partner │ ──► Send partner info to both
           │  info  │
           └────────┘

TRANSACTION SAFETY
- Uses PostgreSQL transactions (BEGIN/COMMIT/ROLLBACK)
- Row-level locking prevents race conditions
- Atomic operations ensure consistency
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                       DATA FLOW                               │
└─────────────────────────────────────────────────────────────┘

PARTICIPANT JOIN
Browser ──POST /api/join──► Backend ──INSERT──► Database
   │                           │                     │
   │                           │                     │
   │       ◄──response─────────┘                     │
   │      {token, slug,                              │
   │       paired, partner}                          │
   │                                                  │
   │                                                  │
   │──────localStorage.setItem('token')              │
   │                                                  │
   │                                                  │
   │──GET /api/status (every 4s)──► Backend          │
   │      with JWT token              │              │
   │                                  │              │
   │                                  │──SELECT──► Database
   │                                  │              │
   │       ◄────response──────────────┘              │
   │      {slug, status,                             │
   │       paired, partner}                          │
   │                                                  │
   │──Display UI update                              │


ADMIN MONITORING
Browser ──GET /api/admin/participants──► Backend
   │      with admin JWT                    │
   │                                         │
   │                                         │──SELECT──► Database
   │                                         │              │
   │       ◄────response──────────────────┘              │
   │      {participants: [...]}                           │
   │                                                       │
   │──Render table                                        │
   │                                                       │
   │──Auto-refresh every 5 seconds                        │


UNPAIR REQUEST
Browser ──POST /api/unpair──► Backend ──UPDATE──► Database
   │      with JWT               │        │          │
   │                             │        │     (set status)
   │                             │        │          │
   │                             │   ◄────┘          │
   │                             │                   │
   │                             │──Try re-pair──► Database
   │                             │     (query)       │
   │       ◄────response─────────┘                   │
   │      {paired, partner}                          │
   │                                                  │
   │──Show toast notification                        │
```

---

**This visual guide shows how all components work together! 🎨**
